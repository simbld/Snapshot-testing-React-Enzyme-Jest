import React from 'react';

const ShowOtherAdviceButton = ({ onClick }) => (
	<button className="Advice-other-advice-button" onClick={onClick}>
		Show other advice
	</button>
);

export default ShowOtherAdviceButton;
